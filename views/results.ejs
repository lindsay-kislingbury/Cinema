<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset = "UTF-8">
    <link rel="stylesheet" href="styles.css">
    <body>
        <!-- Stub to demonstrate how to access the data in the page -->
        <p>
            <b>You chose the following keywords:</b>
            <br>
            <% for(var i=0; i < query.keywords.length; i++) { %>
                <%- query.keywords[i]; %> ,
            <% } %>
            <br><br>
            <b>You chose the following genres:</b>
            <br>
            <% for(var i=0; i < query.genres.length; i++) { %>
                <%- query.genres[i]; %> ,
            <% } %>
        </p>
    </body>
</head>
</html>